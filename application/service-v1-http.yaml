apiVersion: v1
kind: Service
metadata:
  name: simple-app-v1-http
  namespace: simple-app
spec:
  selector:
    app: simple-app
    version: v1
    protocol: http
  ports:
    - port: 80
      targetPort: 5678
