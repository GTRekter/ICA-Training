apiVersion: apps/v1
kind: Deployment
metadata:
  name: simple-app-v3-http
  namespace: simple-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: simple-app
      version: v3
      protocol: http
  template:
    metadata:
      labels:
        app: simple-app
        version: v3
        protocol: http
    spec:
      containers:
        - name: http-app
          image: hashicorp/http-echo:latest
          args:
            - "-text=Simple App v3"
          ports:
            - containerPort: 5678
